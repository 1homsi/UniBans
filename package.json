{
  "name": "conceilax",
  "type": "module",
  "version": "0.0.2",
  "license": "MIT",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "dev": "nodemon --watch server --exec node --experimental-modules server/server.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-async-handler": "^1.1.4",
    "jsonwebtoken": "^8.5.1",
    "mailgun-js": "^0.6.7",
    "mongoose": "^5.10.7",
    "multer": "^1.4.2",
    "socket.io": "^4.0.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
